# MTG Python Deckbuilder

## [Unreleased]
### Added
_No unreleased changes yet_

### Changed
_No unreleased changes yet_

### Fixed
- The CI job that builds the pre-computed card cache now also builds the token/emblem catalog and its printings index, and both files are included in every pre-built data download path (Docker Hub images, container startup seeding, and the web UI's "Download from GitHub" button), so tokens/emblems are actually populated for users who rely on those instead of a full local rebuild.

### Removed
_No unreleased changes yet_

### Security
_No unreleased changes yet_